'use client'

import { useEffect, useState } from 'react'
import Image from 'next/image'
import { ArrowRight, Camera, ChevronDown, ChevronLeft, ChevronRight, Menu, ShieldCheck, Sparkles, X } from 'lucide-react'
import { products, type Product } from '../lib/products'

import Link from 'next/link'

function Header({ onBag }: { onBag: () => void }) {
  return (
    <header className="container-cv flex items-center justify-between py-6">
      <button className="md:hidden" aria-label="Open menu"><Menu size={20}/></button>
      <a href="#top" className="font-display text-2xl tracking-tight">caramel<span className="text-primary">.</span>vibe</a>
      <nav className="hidden gap-8 text-xs font-bold uppercase tracking-widest md:flex">
        <a href="#edit">The edit</a>
        <a href="#story">Our story</a>
        <a href="#faq">Questions</a>
      </nav>
      <div className="flex items-center gap-4">
        <Link href="/login" className="text-xs font-bold uppercase tracking-widest text-muted-foreground hover:text-primary transition-colors">
          Sign In
        </Link>
        <button onClick={onBag} className="flex items-center gap-2 text-xs font-bold uppercase tracking-widest border border-primary/20 px-3 py-1.5 rounded-lg bg-primary/10 text-primary hover:bg-primary/20 transition-colors">
          <span className="hidden sm:inline">Reserve</span>
          <span>(01)</span>
        </button>
      </div>
    </header>
  )
}

function Checkout({ product, open, onClose }: { product: Product; open: boolean; onClose: () => void }) { const [sent, setSent] = useState(false); if (!open) return null; return <div className="fixed inset-0 z-50 flex justify-end bg-foreground/35"><div className="h-full w-full max-w-md overflow-y-auto bg-card p-6 shadow-2xl sm:p-10"><div className="flex items-center justify-between"><p className="eyebrow">Private reservation</p><button onClick={onClose} aria-label="Close"><X size={20}/></button></div>{sent ? <div className="flex min-h-[70vh] flex-col items-start justify-center gap-5"><Sparkles className="text-primary" size={32}/><h2 className="font-display text-5xl">It&apos;s yours.</h2><p className="leading-7 text-muted-foreground">Thank you for choosing {product.name}. We&apos;ll reach out on WhatsApp to confirm your details.</p><button onClick={onClose} className="mt-4 border border-primary px-6 py-3 text-xs font-bold uppercase tracking-widest text-primary">Keep browsing</button></div> : <form onSubmit={(e) => { e.preventDefault(); setSent(true) }} className="mt-8 grid gap-5"><div className="flex items-center gap-4 border-b border-border pb-5"><Image src={product.image} alt="" width={80} height={80} className="h-20 w-20 object-contain"/><div><p className="font-display text-xl">{product.name}</p><p className="text-primary">{product.price}</p></div></div><label className="grid gap-2 text-xs font-bold uppercase tracking-widest">Full name<input required className="border border-border bg-transparent p-3 text-sm font-normal normal-case tracking-normal outline-primary"/></label><label className="grid gap-2 text-xs font-bold uppercase tracking-widest">Email<input required type="email" className="border border-border bg-transparent p-3 text-sm font-normal normal-case tracking-normal outline-primary"/></label><label className="grid gap-2 text-xs font-bold uppercase tracking-widest">Phone / WhatsApp<input required className="border border-border bg-transparent p-3 text-sm font-normal normal-case tracking-normal outline-primary"/></label><label className="grid gap-2 text-xs font-bold uppercase tracking-widest">Shipping address<textarea required rows={3} className="border border-border bg-transparent p-3 text-sm font-normal normal-case tracking-normal outline-primary"/></label><label className="grid gap-2 text-xs font-bold uppercase tracking-widest">Payment method<select className="border border-border bg-transparent p-3 text-sm font-normal normal-case tracking-normal outline-primary"><option>Cash on delivery</option><option>Mobile wallet</option><option>Bank transfer</option></select></label><button className="mt-2 flex items-center justify-center gap-3 bg-primary px-6 py-4 text-xs font-bold uppercase tracking-widest text-primary-foreground">Secure this vibe <ArrowRight size={16}/></button><p className="text-center text-xs leading-5 text-muted-foreground">Frontend reservation only. Our team will confirm availability with you.</p></form>}</div></div> }

export default function Page() { const [index, setIndex] = useState(0); const [open, setOpen] = useState(false); useEffect(() => { const timer = setInterval(() => setIndex((i) => (i + 1) % products.length), 6500); return () => clearInterval(timer) }, []); const product = products[index]; const cta = <button onClick={() => setOpen(true)} className="group flex items-center gap-4 bg-primary px-6 py-4 text-xs font-bold uppercase tracking-widest text-primary-foreground transition-transform hover:-translate-y-1">Secure this vibe <ArrowRight size={17} className="transition-transform group-hover:translate-x-1"/></button>; return <><div id="top"><Header onBag={() => setOpen(true)}/></div><main><section className="container-cv grid min-h-[680px] items-center gap-12 py-12 md:grid-cols-[.9fr_1.1fr] md:py-20"><div className="order-2 md:order-1"><p className="eyebrow">A considered collection · Kabul / Worldwide</p><h1 className="font-display mt-6 max-w-xl text-6xl leading-[.95] tracking-tight md:text-8xl">Good bags have <em className="text-primary">stories.</em></h1><p className="mt-8 max-w-md text-base leading-7 text-muted-foreground">Pre-loved pieces, carefully chosen. For the woman who knows that the best accessory is the one that already has a life.</p><div className="mt-10">{cta}</div><div className="mt-12 flex items-center gap-5 text-xs text-muted-foreground"><ShieldCheck size={18} className="text-accent"/> Every piece authenticated &amp; lovingly inspected</div></div><div className="relative order-1 flex min-h-[440px] items-center justify-center bg-muted md:order-2 md:min-h-[590px]"><div className="absolute left-6 top-6 eyebrow">01 / 0{products.length}</div><Image key={product.image} src={product.image} alt={product.name} width={620} height={620} priority className="h-[400px] w-[90%] object-contain md:h-[540px]"/><div className="absolute bottom-5 left-6 right-6 flex items-end justify-between"><div><p className="font-display text-2xl">{product.name}</p><p className="mt-1 text-sm text-muted-foreground">{product.price}</p></div><div className="flex gap-2"><button aria-label="Previous product" onClick={() => setIndex((index - 1 + products.length) % products.length)} className="border border-border bg-card p-3"><ChevronLeft size={16}/></button><button aria-label="Next product" onClick={() => setIndex((index + 1) % products.length)} className="border border-border bg-card p-3"><ChevronRight size={16}/></button></div></div></div></section><section id="edit" className="border-y border-border py-20"><div className="container-cv"><div className="flex items-end justify-between"><div><p className="eyebrow">The edit</p><h2 className="font-display mt-3 text-5xl">Found, not forced.</h2></div><span className="hidden text-sm text-muted-foreground sm:block">03 pieces in rotation</span></div><div className="mt-12 grid gap-5 md:grid-cols-3">{products.map((p, i) => <button key={p.name} onClick={() => { setIndex(i); setOpen(true) }} className="group text-left"><div className="bg-muted p-5"><Image src={p.image} alt={p.name} width={400} height={400} className="h-64 w-full object-contain transition-transform group-hover:scale-105"/></div><p className="mt-4 font-display text-2xl">{p.name}</p><p className="mt-1 text-sm text-primary">{p.price}</p></button>)}</div></div></section><section id="story" className="container-cv grid gap-12 py-24 md:grid-cols-2 md:items-center"><div><p className="eyebrow">Why Caramel Vibe</p><h2 className="font-display mt-4 text-5xl leading-tight md:text-6xl">The charm is in the <em className="text-primary">history.</em></h2></div><div className="grid gap-8 text-sm leading-7 text-muted-foreground"><p>We believe a handbag should feel like a find — not a transaction. Each piece is selected for its character, quality, and the little details that make it unmistakably yours.</p><div className="grid gap-5 border-t border-border pt-6 sm:grid-cols-2"><div><p className="mb-2 font-bold uppercase tracking-widest text-foreground">01 · Authentic</p><p>Every item is inspected before it reaches you.</p></div><div><p className="mb-2 font-bold uppercase tracking-widest text-foreground">02 · Personal</p><p>One-of-one pieces, chosen for your next chapter.</p></div></div>{cta}</div></section><section className="bg-foreground py-20 text-card"><div className="container-cv grid gap-8 md:grid-cols-3"><div><p className="eyebrow text-accent">Kind words</p><p className="mt-4 font-display text-3xl">“The bag feels like it was waiting for me.”</p><p className="mt-5 text-xs uppercase tracking-widest text-card/60">— Marzia, Kabul</p></div><div><p className="mt-8 font-display text-3xl md:mt-0">“Beautifully wrapped, exactly as pictured.”</p><p className="mt-5 text-xs uppercase tracking-widest text-card/60">— Laila, Herat</p></div><div><p className="mt-8 font-display text-3xl md:mt-0">“My new everyday heirloom.”</p><p className="mt-5 text-xs uppercase tracking-widest text-card/60">— Soraya, Dubai</p></div></div></section><section id="faq" className="container-cv max-w-3xl py-24"><p className="eyebrow">Good to know</p><h2 className="font-display mt-3 text-5xl">Questions, answered.</h2><div className="mt-10 divide-y divide-border">{[['Are the bags authentic?','Yes. Every piece is carefully inspected and authenticated before listing.'],['How does delivery work?','We coordinate delivery details privately after your reservation, with shipping available worldwide.'],['Can I return a piece?','Because each piece is one-of-one, we ask you to review all details before confirming. Our team is here to help.']].map(([q,a]) => <details key={q} className="group py-5"><summary className="flex cursor-pointer list-none items-center justify-between font-display text-xl">{q}<ChevronDown size={18} className="transition-transform group-open:rotate-180"/></summary><p className="max-w-xl pt-4 text-sm leading-7 text-muted-foreground">{a}</p></details>)}</div></section></main><footer className="border-t border-border py-8"><div className="container-cv flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><p className="font-display text-2xl">caramel<span className="text-primary">.</span>vibe</p><a href="https://instagram.com" className="flex items-center gap-2 text-xs font-bold uppercase tracking-widest"><Camera size={16}/> Follow the find</a><p className="text-xs text-muted-foreground">© 2026 Caramel Vibe</p></div></footer><Checkout product={product} open={open} onClose={() => setOpen(false)}/></> }
